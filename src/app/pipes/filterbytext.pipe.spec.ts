import { FilterbytextPipe } from './filterbytext.pipe';

describe('FilterbytextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbytextPipe();
    expect(pipe).toBeTruthy();
  });
});
